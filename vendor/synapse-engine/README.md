# synapse-engine (vendored placeholder)

This directory is vendored into the monorepo to satisfy local workspace references.
Network restrictions prevented cloning `pmaojo/synapse-engine` during automation.
